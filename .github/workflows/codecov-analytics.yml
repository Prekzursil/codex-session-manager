name: Codecov Analytics

permissions:
  contents: read
  id-token: write

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  shared-codecov-analytics:
    permissions:
      contents: read
      id-token: write
    uses: Prekzursil/quality-zero-platform/.github/workflows/reusable-codecov-analytics.yml@e2b78ce090424b67f9f7e30e8c30770f4be4c054
    with:
      repo_slug: ${{ github.repository }}
      event_name: ${{ github.event_name }}
      sha: ${{ github.event.pull_request.head.sha || github.sha }}
      runner: windows-latest
      platform_repository: Prekzursil/quality-zero-platform
      platform_ref: e2b78ce090424b67f9f7e30e8c30770f4be4c054
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
